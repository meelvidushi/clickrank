We generated 20 example queries using LLAMA 3.1 Instruct 8B however some example queries
are terrible so we manually pruned them. An example would be "near me" as a query for a page
describing car repair shops.
